# Installation

## Prerequisites

Ream requires Node.js 8 or above, you can run following command to check its version:

```bash
node -v
```

## Install locally

Use Yarn:

```bash
cd my-project
yarn add ream
```

Use npm:

```bash
cd my-project
npm i ream
```


